nav.navbar.navbar-dark.bg-dark.navbar-expand-md.fixed-top.flex-md-nowrap
  a.home-logo.navbar-brand(href='/') ECL IDE
  button.navbar-toggler(type='button', data-toggle='collapse', data-target='#main-nav'): span.navbar-toggler-icon
  div#main-nav.collapse.navbar-collapse
    ul.navbar-nav.mr-auto
      li.nav-item.dropdown
        a#workspaceSelect.nav-link.dropdown-toggle(href='#', role='button', data-toggle='dropdown', aria-haspopup='true', aria-expanded='false') Select Workspace...
        div.dropdown-menu.workspaces(aria-labelledby='workspaceSelect')
          a.dropdown-item.cloner.d-none(href='#') Workspace
          .workspace-tip.ml-3.mr-2.d-none
            p.m-0.float-left No workspaces yet.#[br] Create one now.
            span.float-right
              i.mt-1.mr-2.fa.fa-2x.fa-arrow-up
      li.nav-item
        a.workspace-btn.new-workspace.text-light.badge.badge-pill.badge-secondary.float-left(href='#', title="New Workspace...", data-toggle="modal", data-target="#newWorkspaceModal") NEW
            i.fa.fa-plus.ml-1.float-right
      li.nav-item.d-none
        a.workspace-btn.delete-workspace.text-light.badge.badge-pill.badge-danger.float-left(href='#', title="Delete Workspace...", data-toggle="modal", data-target="#removeWorkspaceModal") DELETE
    ul.navbar-nav.ml-md-auto
      li.nav-item
        a.nav-link(href='/users/account') Account
          i.fa.fa-lg.fa-user.float-left.mr-2
      li.nav-item
        a.nav-link(href='/auth/logout') Logout
          i.fa.fa-lg.fa-sign-out.float-left.mr-2

div.modal.fade(id="newWorkspaceModal", role="dialog")
  div.modal-dialog
    div.modal-content
      div.modal-header
        h5.modal-title New Workspace
        button.close(data-dismiss="modal", aria-label="Close")
          span(aria-hidden="true") &times;
      div.modal-body
        form
          div.form-group
            label.col-form-label(for="workspace-name") Name:
            input.form-control(id="workspace-name", name="workspaceName", type="text", required="required", placeholder="MyWorkspace...")
            .invalid-feedback Please provide a name for the workspace.
          div.form-group
            label.col-form-label(for="workspace-cluster") Cluster:
            input.form-control(id="workspace-cluster", name="workspaceCluster", type="text", required="required", placeholder="URL or IP:port, such as \"10.173.147.1:8010\"...")
            .invalid-feedback Please provide a valid url for the cluster.
      div.modal-footer
        button.btn.btn-secondary(data-dismiss="modal") Cancel
        button.btn.btn-primary Save

div.modal.fade(id="editWorkspaceModal", role="dialog")
  div.modal-dialog
    div.modal-content
      div.modal-header
        h5.modal-title Edit Workspace
        button.close(data-dismiss="modal", aria-label="Close")
          span(aria-hidden="true") &times;
      div.modal-body
        form
          div.form-group
            label.col-form-label(for="edit-workspace-name") Name:
            input.form-control(id="edit-workspace-name", name="workspaceName", type="text", required="required", placeholder="MyWorkspace...")
            .invalid-feedback Please provide a name for the workspace.
          div.form-group
            label.col-form-label(for="edit-workspace-cluster") Cluster:
            input.form-control(id="edit-workspace-cluster", name="workspaceCluster", type="text", required="required", placeholder="URL or IP:port, such as \"10.173.147.1:8010\"...")
            .invalid-feedback Please provide a valid url for the cluster.
      div.modal-footer
        button.btn.btn-secondary(data-dismiss="modal") Cancel
        button.btn.btn-primary Save

div.modal.fade(id="removeWorkspaceModal", role="dialog")
  div.modal-dialog
    div.modal-content
      div.modal-header
        h5.modal-title Delete Workspace
        button.close(data-dismiss="modal", aria-label="Close")
          span(aria-hidden="true") &times;
      div.modal-body
        p Delete the workspace "#[span.workspacename workspace]"?

      div.modal-footer
        button.btn.btn-secondary(data-dismiss="modal") Cancel
        button.btn.btn-danger Delete
